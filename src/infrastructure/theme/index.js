import { colors } from "./colors";
import {
  space,
  lineHeights,
  spaceInNumber,
  lineHeightsInNumber,
} from "./spacing";
import { sizes, sizesInNumber } from "./sizes";
import { fonts, fontWeights, fontSizes, fontSizesInNumber } from "./fonts";

export const theme = {
  colors,
  space,
  spaceInNumber,
  lineHeights,
  lineHeightsInNumber,
  sizes,
  sizesInNumber,
  fonts,
  fontSizes,
  fontSizesInNumber,
  fontWeights,
};
